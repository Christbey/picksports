[build-system]
requires = ["setuptools==75.6.0", "wheel==0.45.1"]
build-backend = "setuptools.build_meta"

[project]
name = "picksports-nfl-ml"
version = "0.1.0"
description = "Self-contained NFL model training and inference package for Picksports"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
    "joblib==1.4.2",
    "numpy==1.26.4",
    "optuna==4.1.0",
    "pandas==2.2.3",
    "pyarrow==18.1.0",
    "PyYAML==6.0.2",
    "scikit-learn==1.5.2",
    "shap==0.46.0",
    "xgboost-cpu==2.1.3; platform_system == 'Linux'",
    "xgboost==2.1.3; platform_system != 'Linux'",
]

[project.optional-dependencies]
test = ["pytest==8.3.4"]

[project.scripts]
picksports-nfl-ml = "picksports_nfl_ml.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
